spring:
  mvc:
    view:
      prefix: /WEB-INF/jsp/
      suffix: .jsp

logging:
  level:
    org.springframework: INFO
    com.yarmak: INFO

---
spring:
  profiles: test

logging:
  level:
    org.springframework: DEBUG

---
spring:
  profiles: dev

logging:
  level:
    org.springframework: ERROR

